const carMakers: string[] = ["ford", "toyot", "chevy"];
const dates = [new Date(), new Date()];

const carsByMake = [["kk"], ["g"], ["j"]];
const car = carMakers[0];
carMakers.map((car: string): string => {
  return car.toUpperCase();
});

//flexible types
